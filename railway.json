{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Monorepo detected - deploy individual services only'",
    "installCommand": "echo 'No installation needed for monorepo root'"
  },
  "deploy": {
    "startCommand": "echo 'DO NOT DEPLOY FROM ROOT - Use GAMMA/backend/ and GAMMA/frontend/ services instead' && exit 1",
    "healthcheckPath": "/",
    "healthcheckTimeout": 5,
    "restartPolicyType": "NEVER"
  }
}
